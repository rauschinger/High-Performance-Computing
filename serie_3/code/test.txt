hello world 

alaa
for(int i=1; i<argc; i++) {
    if     (i<argc-1 && strcmp(argv[i], "-N"          ) == 0) { N           = atoi(argv[i+1]); i++; }
    else if(i<argc-1 && strcmp(argv[i], "-N"          ) == 0) { N           = atoi(argv[i+1]); i++; }
    else {
        std::cout << "Command '" << argv[i] << "' unknown and will be ignored" << std::endl;
    }
}

